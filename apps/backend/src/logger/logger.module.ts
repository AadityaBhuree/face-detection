import { Module } from '@nestjs/common';

/**
 * LoggerModule exists for documentation/scaffolding purposes.
 * Logger is always instantiated directly via `new Logger(name)`
 * and does not go through NestJS DI.
 */
@Module({})
export class LoggerModule {}
